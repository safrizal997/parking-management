server:
  port: 8080

spring:
  application:
    name: Parking-Management

  jpa:
    database: mysql
    show-sql: false
    hibernate:
      ddl-auto: update
    open-in-view: false

  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:3306/db_parkingmanagement
    username: root
    password: root

